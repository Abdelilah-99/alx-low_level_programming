#include "main.h"
/**
 * _isdigit - with the function isdigit
 * we search if c is a digit or not
 * @c: c will return 1 if the valu of
 * c is a digi if is not will return 0
 * Return: Returns 0
 */
int _isdigit(int c)
{
	if (isdigit(c))
		return (1);
	else
		return (0);
	return (0);
}
