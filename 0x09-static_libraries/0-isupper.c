#include "main.h"
/**
 * _isupper - that function used for know is the digit a uppercase or not
 * @c: if c a uppercase charactere the return will be 1 if not return 0
 * Return: Returns 0
 */
int _isupper(int c)
{
	if (isupper(c))
		return (1);
	else
		return (0);
	return (0);
}
